<h1>Haha it worked</h1>
<div> Welcome <%= @user.name %> </div>
<% @user.events.each do |event| %>
    <div>
        <div><%= event.name%></div>
        <div><%= event.description%></div>
    </div>
<% end %>